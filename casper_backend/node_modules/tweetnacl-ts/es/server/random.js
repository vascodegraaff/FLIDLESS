import { randomBytes } from 'crypto';
export function _randomBytes(x, n) {
    var v = randomBytes(n);
    for (var i = 0; i < n; i++) {
        x[i] = v[i];
        v[i] = 0;
    }
}
//# sourceMappingURL=random.js.map